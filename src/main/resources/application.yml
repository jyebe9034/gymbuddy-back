server:
  port: 8080

spring:
  datasource:
    #url: jdbc:mariadb://localhost:3306/gymbuddy   #개발
    url: jdbc:mariadb://localhost:3306/gymbuddy   #운영
    driver-class-name: org.mariadb.jdbc.Driver
    username: gymbuddy
    password: myang2526!
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: none
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 1MB
      max-file-size: 500MB
      max-request-size: 500MB
  main:
    allow-bean-definition-overriding: true

